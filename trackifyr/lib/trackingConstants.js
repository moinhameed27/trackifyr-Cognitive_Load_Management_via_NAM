/** Rows per page for session logs (dashboard + API default) */
export const SESSION_LOG_PAGE_SIZE = 10

/** Rolling window for weekly dashboard chart (PKT calendar days) */
export const WEEKLY_ROLLING_DAYS = 7
